#!/bin/bash
# Delete local branches merged into current HEAD
current_branch=$(git branch --show-current)
echo "🧹 Cleaning merged branches (excluding $current_branch)..."

git branch --merged | grep -v "\*" | grep -v "$current_branch" | xargs -n 1 git branch -d

echo "✨ All merged branches deleted."
